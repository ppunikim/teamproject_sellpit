<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.callor.school.pesistance.UserDao">
	<select id="selectAll" resultType="UserVO">
		SELECT * FROM tbl_login
	</select>
	
	<select id="findById" resultType="UserVO">
		SELECT * FROM tbl_login WHERE username = #{username}
	</select>
	
	<insert id="insert" parameterType="UserVO">
		INSERT INTO tbl_login
			(
				username, 
				password, 
				name, 
				email,
				gender, 
				role
			) VALUES (
				#{username}, 
				#{password}, 
				#{name}, 
				#{email},
				#{gender},
				#{role}
			)
	</insert>
	<update id="update" parameterType="UserVO">
		UPDATE tbl_login SET
				password = #{password}, 
				name = #{name},
				email = #{email},
				gender = #{gender},
				role = #{role}
		WHERE username = #{username} 
	</update>
	<delete id="delete">
		DELETE FROM tbl_login WHERE username = #{username}
	</delete>
</mapper>





